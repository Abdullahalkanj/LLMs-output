| Check | Item                           | What to Check                                                                                                                                                 | Why                                                                                                      |
| :---: | ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
|   ✔️  | Data Model & Structure         | `Node` inner class with `element`, `prev`, `next`; sentinel `header`/`trailer`; and `size` in `DoublyLinkedBase`.                                             | Validates the doubly linked list backbone and sentinel setup for boundary simplification.                |
|   ✔️  | Length & Emptiness             | `__len__` returning `self.size` and `is_empty` checking `self.size == 0`.                                                                                     | Ensures the deque correctly reports its current size and empty status.                                   |
|   ✔️  | `insert_between` Logic         | Creating a new node between given predecessor and successor, updating their pointers, and incrementing `size`.                                                | Confirms correct insertion mechanics and maintenance of list integrity and element count.                |
|   ✔️  | `delete_node` Logic            | Unlinking the node’s `prev`/`next` pointers, decrementing `size`, nullifying node’s fields, and returning its `element`.                                      | Checks safe removal, size update, and cleanup to prevent dangling references and aid garbage collection. |
|   ✔️  | `first` & `last` Accessors     | Peeking at `header.next.element` or `trailer.prev.element`, with `Empty` exception if deque is empty.                                                         | Verifies correct front/back retrieval and consistent underflow error handling.                           |
|   ✔️  | `insert_first` & `insert_last` | Delegating to `insert_between` with `header`/`header.next` or `trailer.prev`/`trailer` to add elements at the front or back.                                  | Ensures high‐level API for adding to both ends uses the low‐level insertion correctly.                   |
|   ✔️  | `delete_first` & `delete_last` | Checking emptiness then delegating to `delete_node` on the first/last real node (`header.next`/`trailer.prev`).                                               | Confirms removal from both ends with proper underflow checks and pointer updates.                        |
|   ✔️  | Underflow Exception            | Raising `Empty("Deque is empty")` in `first`, `last`, `delete_first`, and `delete_last` when deque is empty.                                                  | Ensures consistent and clear error signaling for illegal operations on an empty deque.                   |
|   ✔️  | Sentinel Benefits              | Use of sentinel `header` and `trailer` nodes to avoid special‐case `None` checks during insertions/deletions at either end.                                   | Highlights how sentinels simplify edge‐case logic and make code more robust and concise.                 |
|   ✔️  | Demo & Usage                   | `__main__` block: creates a deque, inserts `[1, 2, 3]` via `insert_last`, then exercises `first`, `last`, `delete_first`, `delete_last`, and prints `len(D)`. | Validates that the example demonstrates the intended deque behavior and state transitions effectively.   |
